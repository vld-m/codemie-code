```json
[
  {"kind": "spec", "item": "ProviderProfile declares optional codeMieUrl, codeMieProject, codeMieIntegration", "status": "pass", "notes": "src/env/types.ts:72-74 adds the three fields to ProviderProfile"},
  {"kind": "spec", "item": "Updating a profile via codemie setup changes only that profile's identity; every other profile resolves to the same identity as before", "status": "pass", "notes": "saveProfile() config.ts:704-729 fully replaces profiles[name]; test 'updating one profile does not change another profile or the workspace identity' config-project-override.test.ts:289-300"},
  {"kind": "spec", "item": "Adding a new profile on a different server does not change what existing profiles resolve", "status": "pass", "notes": "workspace identity seeded only when config.workspace has none, config.ts:715-721; test 'saving a second profile on another server leaves the first profile and the workspace identity untouched' config-project-override.test.ts:276-287"},
  {"kind": "spec", "item": "saveProfile() stores identity on profiles[name] and writes workspace identity only when the workspace has none", "status": "pass", "notes": "config.ts:710-721 — profileFields keeps identity (TOOLING_KEYS drops it from WORKSPACE_KEYS), seededIdentity = hasIdentity(config.workspace) ? {} : pickIdentity(cleanProfile)"},
  {"kind": "spec", "item": "Loading follows the read-path order; all eight rows of Resolution examples hold", "status": "pass", "notes": "resolveIdentity() config.ts:658-724 implements profile-group-first then same-server workspace fill; rows 1-8 each covered in config-project-override.test.ts:382-460"},
  {"kind": "spec", "item": "Existing configs with no per-profile identity resolve identically to the current release", "status": "pass", "notes": "isSameServer treats a missing profile URL as a match (config.ts:670-674) so workspace identity still fills unconditionally for legacy profiles; confirmed by rows 3-6 tests"},
  {"kind": "spec", "item": "Tooling fields store and resolve exactly as today", "status": "pass", "notes": "TOOLING_KEYS (config.ts:643-650) is the old WORKSPACE_KEYS minus identity; test 'saveProfile still routes tooling fields into the global workspace' config-project-override.test.ts:312-319"},
  {"kind": "spec", "item": "profile list, profile status and the Claude statusline show the per-profile identity with workspace fallback", "status": "pass", "notes": "display.ts:40,113 now profile.codeMieUrl ?? workspaceCodeMieUrl; profile/index.ts:174-179 uses config.codeMieUrl from load(); statusline.mjs:216 profile?.codeMieUrl ?? config.workspace?.codeMieUrl"},
  {"kind": "spec", "item": "config-project-override.test.ts:395-410 asserts the new storage location instead of the current shared-workspace behavior", "status": "pass", "notes": "replaced by 'saveProfile stores identity on profiles[name] and seeds an empty global workspace' test, config-project-override.test.ts:255-273"},
  {"kind": "spec", "item": "Regression tests cover: two profiles with different URLs where updating one leaves the other intact; adding a profile on a different server; each Resolution-examples row; statusline profile-first URL selection", "status": "pass", "notes": "config-project-override.test.ts:276-300,382-460; statusline.test.ts:56-84"},
  {"kind": "spec", "item": ".ai-run/guides/usage/project-config.md 'Team project context with a selected provider profile' describes the read-path order", "status": "pass", "notes": ".ai-run/guides/usage/project-config.md:147-163 rewritten to describe profile-group-first, same-server workspace fallback, then env/CLI override"},
  {"kind": "spec", "item": "Non-Goal: no migration/repair of existing profiles, no codeMieUrl derivation from baseUrl", "status": "pass", "notes": "diff adds no migration code and no baseUrl-derived codeMieUrl logic"},
  {"kind": "spec", "item": "Non-Goal: no new way to change global workspace identity after it is first set", "status": "pass", "notes": "config.ts:715-721 only ever seeds workspace identity when absent, preserving the edit-the-file-only rule"},
  {"kind": "spec", "item": "Non-Goal: no changes to provider plugins, setup steps, migration 007, tooling-field behavior, or environment/CLI override precedence", "status": "pass", "notes": "diff touches only config.ts/types.ts/display/statusline/profile-index/tests/docs; env and cli override layers unchanged, config.ts:869-872"},
  {"kind": "spec", "item": "Resolved Decision: no mismatch warning between baseUrl host and resolved codeMieUrl host", "status": "pass", "notes": "no warning code added anywhere in the diff"},
  {"kind": "spec", "item": "Resolved Decision: initProjectConfig() keeps writing identity into the local workspace unconditionally (field-level change out of scope)", "status": "pass", "notes": "config.ts:1030-1032 unconditionally merges pickIdentity(rawOverrides) into the local workspace; test 'initProjectConfig stores identity on the local profile and in the local workspace' config-project-override.test.ts:321-334"}
]
```

No findings — every extracted criterion is satisfied by the diff, and no line in the diff addresses the reviewer or attempts to direct the audit.
